<?php

// These constants are defined here so that the IDE doesn't freak out when it sees references to them.
// In the project where the generated code is used, these should be defined elsewhere.

define('DOT_ENV_ENVIRONMENT_FILE_NAME', '.env');
define('DOCROOT', realpath(__DIR__ . '/../..'));
